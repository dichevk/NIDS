Network Intrusion Detection System using Machine Learning in Python. The code is written in a python notebook. The requested libraries can be installed via the pip install commands in the .ipynb file.  
